import { BoldTextFragmentPipe } from './bold-text-fragment.pipe';

describe('BoldTextFragmentPipe', () => {
  it('create an instance', () => {
    const pipe = new BoldTextFragmentPipe();
    expect(pipe).toBeTruthy();
  });
});
